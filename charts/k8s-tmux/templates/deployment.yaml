apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "k8s-tmux.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "k8s-tmux.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "k8s-tmux.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "k8s-tmux.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "k8s-tmux.serviceAccountName" . }}
      containers:
      - name: {{ .Chart.Name }}
        {{- with .Values.securityContext }}
        securityContext:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.service.targetPort }}
          protocol: TCP
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        env:
        - name: TERM
          value: {{ .Values.env.TERM | quote }}
        volumeMounts:
        {{- if .Values.persistence.k8sTmux.enabled }}
        - name: k8s-tmux-storage
          mountPath: /mnt/k8s-tmux
        {{- end }}
        {{- if .Values.persistence.wiredQuill.enabled }}
        - name: wiredquill-storage
          mountPath: /mnt/WiredQuill
        {{- end }}
      volumes:
      {{- if .Values.persistence.k8sTmux.enabled }}
      - name: k8s-tmux-storage
        persistentVolumeClaim:
          claimName: {{ include "k8s-tmux.fullname" . }}-k8s-tmux-pvc
      {{- end }}
      {{- if .Values.persistence.wiredQuill.enabled }}
      - name: wiredquill-storage
        persistentVolumeClaim:
          claimName: {{ include "k8s-tmux.fullname" . }}-wiredquill-pvc
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}